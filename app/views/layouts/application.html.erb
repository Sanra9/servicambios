<!DOCTYPE html>
<html>
  <head>
    <title>Servicambios</title>
    <%= favicon_link_tag asset_path('favicon.ico'), :rel => 'icon', :type => "image/x-icon" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
   <nav class="navbar navbar-light navbar-fixed-top" style="background-color: rgba(227,242,253,0.5);">
        <div class="container">
          <div class="navbar-header">
           <%= link_to "<i>Servi</i><strong>Cambios</strong>".html_safe, root_path, class:"navbar-brand" %>
          </div>
          <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
            <%= link_to "Salir", destroy_user_session_path, method: :delete, class:"navbar-brand"%>
            <% else %>
            <%= link_to "Regístrate", new_user_registration_path, class:"navbar-brand"%>
            <%= link_to "Login", new_user_session_path, class:"navbar-brand" %>
          <% end %>
          </ul>
          </div>
    </nav>
  <div class='myAlert-bottom'>
  <div class='container'>
    <div class='row'>
        <div class='col-sm-3 col-sm-offset-3'>
          <% if flash[:notice] %>
            <div class='alert alert-success'><%= flash[:notice]%>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
             </button>
            </div>
          <%end%>
        </div>
     </div>
  </div>
 </div> 
    <%= yield %>
  </body>
</html>